{
  "name": "zeldaish-videogame",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "devDependencies": {
    "@types/node": "^22.15.21",
    "@biomejs/biome": "^1.9.4",
    "tsx": "^4.7.0",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "start:cli": "pnpm --filter cli run",
    "start:game": "pnpm --filter game dev",
    "typecheck": "pnpm -r exec tsc --noEmit"
  }
}
